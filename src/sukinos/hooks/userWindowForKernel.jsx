import { useState, useEffect, useCallback } from "react"
import kernel from "@/sukinos/utils/process/kernel"

export const useWindowForKernel = (pid) => {
  //这里主要就是直接将这个暴露出去,将app能够是西安更多的权限处理
  return {  }
}

export default useWindowForKernel
